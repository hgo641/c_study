#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "book.h"
#pragma warning(disable : 4996)

void main() {
	// 파일입력
	/*
	char title[20];
	printf("1입력 : ");
	scanf("%s", &title);
	char author[10];
	printf("2입력 : ");
	scanf("%s", &author);
	int num;
	printf("3입력 : ");
	scanf("%d", &num);
	char r = 'X';
	//FILE* fp = fopen("book_list.txt", "w");
	FILE* fp = fopen("book_list.txt", "a");
	fprintf(fp, "%s\t%s\t%d\t%c\n",title,author,num,r);

	fclose(fp);
	*/

	menu();
	
	
		return 0;
		
	}

